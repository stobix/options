{application, options,
    [{description, "A utility for handling options taken from a config file, the environment or the -config erl option."},
        {vsn,"1.0"},
        {modules,[options,options_sup]},
        {registered,[options]},
        {applications,[kernel,stdlib]},
        {mod,{options,[]}},
        {env,[]}]}.

