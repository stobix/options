{application, options,
    [{description, "A utility for handling (module specific or general) options taken from config files, the environment or the -config_file erl argument."},
        {vsn,"0.0.2"},
        {modules,[options,options_sup]},
        {registered,[options]},
        {applications,[kernel,stdlib,newdebug]},
        {mod,{options,[]}},
        {env,[]}]}.

